name: "CodeQL Analysis: cFS-Bundle"

# There are still some bugs with this workflow, so keeping
# it as manual dispatch for now until they can be fixed.
on:
  workflow_dispatch:

jobs:
  codeql:
    uses: ./.github/workflows/codeql-reusable.yml
    with:
      component-path: cFS
      prep: make native_std.prep
      make: make native_std.install
      test: true
