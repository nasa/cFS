name: "CodeQL Analysis: cFS-Bundle"

on:
  push:
    branches:
      - dev
      - main
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
  workflow_dispatch:

jobs:
  codeql:
    uses: ./.github/workflows/codeql-reusable.yml
    with:
      component-path: cFS
      make: make native_std.install -j8
      test: true
